# preprocess_tennis

Pre-processing scripts for Tennis Transformer model (binary video classification). `UnifromDataset.py` given an Excel file containing full videos and their segments' labels, creates videos of uniform size (t frames) and saves each of their corresponding label.
